module.exports = {
  apps: [
    {
      name: "hoshino",
      exec_mode: "fork",
      script: "bun",
      args: "run index.js",
      watch: true,
      watch_delay: 500,
      ignore_watch: [
        "node_modules",
        "databases",
        "auth",
        "configs",
        "src/temp",
      ],
      autorestart: true,
      max_memory_restart: "300M",
      restart_delay: 1000,
      env: {
        NODE_ENV: "development",
      },
      env_production: {
        NODE_ENV: "production",
      },
    },
  ],
};