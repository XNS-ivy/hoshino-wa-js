module.exports = {
  apps: [
    {
      name: "hoshino",
      script: "./index.js",
      interpreter: "bun",
      exec_mode: "fork",
      watch: true,
      autorestart: true,
      max_memory_restart: "300M",
      restart_delay: 1000,
      env: {
        NODE_ENV: "development",
      },
      env_production: {
        NODE_ENV: "production",
      },
    },
  ],
}